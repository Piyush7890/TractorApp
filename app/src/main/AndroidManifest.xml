<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.wce.tractorapp">

    <uses-permission android:name="android.permission.CALL_PHONE" />

    <application
        android:name=".TractorApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".GenerateReceiptActivity">   <intent-filter>
            <action android:name="android.intent.action.MAIN" />

            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter></activity>
        <activity android:name=".SelectActivity" />
        <activity android:name=".SearchActivity" />
        <activity
            android:name=".AddProductActivity"
            android:theme="@style/AddProductActivity" />
        <activity android:name=".ConversationActivity" />
        <activity
            android:name=".DetailsActivity"
            android:theme="@style/DetailsActivity" />
        <activity android:name=".main.MainActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".TrampolineActivity">

        </activity>
    </application>

</manifest>